# 续航助手 3.1.0 发布说明

## 本次修改

- 设备页改为运行时读取型号、CPU、内存、存储、GPU、Windows 与电池数据，不再包含固定电脑配置。
- CPU 能效偏好改为逐项能力探测，只修改可读取、可写入并能回读的设置。
- CPU EPP 不可用时自动降级为设备支持的关屏策略，能力状态显示在设备页。
- 增加 `--restore-and-exit`，覆盖安装和卸载时自动恢复原电源方案并删除临时方案。
- 版本统一为 3.1.0，补齐 EXE、manifest 和安装器版本元数据。
- 加入标准 .NET Framework 4.8 项目、MIT 许可证、README、适配文档和 Inno Setup 源码。
- 安装器采用每用户安装，不要求管理员权限；创建开始菜单快捷方式，桌面快捷方式可选。
- 安装包保留旧版快捷方式使用的图标文件名，便于从手动安装平滑升级。

## 适配范围

| 环境 | 状态 | 说明 |
| --- | --- | --- |
| Windows 11 22H2 及更新版本 | 正式支持 | 系统 Acrylic 需要 Build 22621；不可用时回退实色 |
| x64 Intel 笔记本 | 支持 | 已在 Core Ultra 5 338H 上验证 3/3 EPP 设置 |
| x64 AMD 笔记本 | 条件支持 | 按 Windows 实际暴露的 EPP/关屏能力运行，尚未完成实体 AMD 设备验证 |
| 无完整电池 WMI 数据的设备 | 降级支持 | 功率、容量或循环次数字段可能为空，进程监测仍可用 |
| 企业策略限制 `powercfg` | 仅监测 | 开启调度可能失败并显示系统返回的错误，不会绕过策略 |
| Windows 10 | 未列入正式支持 | 核心 API 可能工作，但 UI 材质和设备矩阵未验证 |
| ARM64、32 位 Windows | 不支持 | 当前产物为 x64；未提供 ARM64 原生版本 |
| macOS、Linux | 不支持 | 依赖 Windows 电源、WMI、DWM 和进程 API |

运行依赖为 Windows 自带的 .NET Framework 4.8/4.8.1、WPF、WMI、`powercfg.exe`、`powrprof.dll`、`kernel32.dll` 和 `dwmapi.dll`。不需要联网、账号、数据库或随包安装第三方运行库。

## 安装与升级行为

- 默认目录：`%LOCALAPPDATA%\Programs\BatteryPilot`
- 用户数据：`%LOCALAPPDATA%\BatteryPilot`
- 升级前关闭正在运行的应用；启动新版前检查并恢复遗留电源方案。
- 卸载前调用恢复命令；恢复失败时中止卸载并保留应用文件。用户设置默认保留，方便重新安装。
- 不设置开机启动，不修改充电上限，不结束其他进程。

## 验证结果

- 应用和安装器编译成功，EXE 产品版本为 3.1.0。
- 自测通过：策略阈值与回差、CPU 差分采样、225 个可读进程、3 个受支持 EPP 项的四档写入/回读、交流设置保持、恢复记录生命周期、原方案保持。
- UI 测试通过：四页导航、筛选、分组、排序、动态设备信息；DWM Acrylic 返回成功，WPF 渲染等级 2。
- 安装闭环通过：静默安装、已安装程序启动、恢复命令、静默卸载均返回 0；测试安装目录被移除，没有遗留恢复记录。
- 卸载保护通过：模拟恢复程序返回 1 时，卸载器返回非零并保留全部应用文件；恢复程序正常后再次卸载成功。

## 发布注意

本地生成的 EXE 和 Setup 当前没有商业代码签名证书，因此文件状态为 `NotSigned`。直接从互联网下载时，Windows 可能显示信誉或未知发布者提示。源码和安装脚本可以公开；正式大规模分发前建议使用受信任证书同时签名主程序和安装包。
