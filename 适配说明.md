# 续航助手 3.1.0 适配说明

## 支持

- Windows 11 22H2 或更新版本
- x64 处理器
- .NET Framework 4.8 或 4.8.1
- 带电池的笔记本、二合一设备

## 自动降级

- 设备只提供部分 CPU 能效偏好设置时，仅写入可读取、可回读的项目。
- CPU 能效偏好完全不可用时，保留设备支持的关屏调度，并在设备页显示“0/3”。
- 电池 WMI 不提供功率、容量或循环次数时，对应字段显示不可用。
- Windows 透明材质不可用、远程桌面或系统关闭透明效果时，界面回退为实色。

## 尚未支持

- 32 位 Windows
- ARM64 原生版本
- Windows 10 的正式支持
- macOS 和 Linux

安装、升级和卸载会先关闭正在运行的程序，并调用恢复命令，避免把 BatteryPilot 创建的临时电源方案遗留在系统中。用户设置默认保留在 `%LOCALAPPDATA%\BatteryPilot`，便于重新安装后继续使用。
