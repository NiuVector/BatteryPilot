# 续航助手 v3.1.0

面向 Windows 11 x64 笔记本的本地应用。本版使用 WPF 保留式渲染与 Windows 原生 Desktop Acrylic；材质不可用时自动回退为实色。页面常驻、进程表虚拟化并增量更新。设置页提供玻璃材质与短动效开关，修改后点击保存。拖动整个顶部区域（包括空白处）可移动窗口，双击顶部可最大化或还原；右上角减号收起到托盘，叉号恢复并退出。

## 打开与使用

双击桌面的“续航助手”快捷方式即可。启动时仅监测，点击概览页“开启均衡调度”才开始调整电源策略。点击“停止并恢复”，或关闭窗口，恢复启用前的电源方案。需要后台运行时，请点击“收起到托盘”，双击托盘图标重新打开。

Setup 默认安装于 `%LOCALAPPDATA%\Programs\BatteryPilot`，开始菜单快捷方式自动创建，桌面快捷方式可选。`BatteryPilot.exe` 也可直接运行。没有设置开机启动。

## 四个页面

- **概览**：电量、整机放电瓦数、近期平均功耗、预估剩余时间、最长 30 分钟、自适应时间范围的功耗曲线、CPU 活跃程序、会话 CSV 导出。
- **进程活动**：按 CPU / 内存 / I/O 排序，搜索名称或 PID，合并同名进程，双击查看 PID 列表，一键打开任务管理器。
- **电源策略**：流畅优先、均衡续航、续航优先；自定义闲置关屏时间、低电量阈值，以及是否自动加强节能。修改后点击“保存并应用”。
- **设备信息**：动态读取本机型号、处理器、内存、存储、显示适配器、电池信息和调度兼容性；生成最新 Windows 电池报告，打开系统电源设置。

进程页每 5 秒采样，其他前台页面每 15 秒采样；收起后暂停进程采样。电池和调度每 15 秒检查一次。新进程需两个样本才能计算变化率。受保护、无权限或已退出的进程可能无法读取，界面会显示不可访问数量。

## 怎样理解“耗电进程”

本版显示的是用于排查耗电的**进程资源活动**，没有把它伪装成精确的进程功耗：

- 整机瓦数来自电池传感器，包含屏幕、处理器等整个系统的耗电。
- CPU 百分比来自相邻采样的处理器时间差，除以时间间隔和逻辑处理器数量，按整机容量归一。
- 内存显示工作集；合并多个进程时，共享页可能重复计算。内存占用本身不是耗电量。
- I/O 为进程读写字节变化率，可包含文件、网络等，不等于物理磁盘流量。
- 活动提示使用简单规则：CPU ≥ 8% 为“CPU 较高”，否则 I/O ≥ 2MB/s 为“读写活跃”，否则 CPU ≥ 1% 为“CPU 活跃”。这些阈值只用于提示，不是能耗测量。
- 不包含 GPU 利用率或每进程真实瓦数，因此 GPU 密集型应用可能在此列表中看起来不活跃。

预估剩余时间 = 剩余电池能量 / 最近连续放电样本的平均功率；最多取近 5 分钟，至少需要两个样本。插电或传感器缺少有效数据时不估算。它随任务负载变化，不能作为续航承诺。

## 调度与恢复

| 条件 | 行为 |
| --- | --- |
| 流畅优先 | 处理器能效偏好 50 |
| 均衡续航（默认） | 处理器能效偏好 60 |
| 续航优先 | 处理器能效偏好 75 |
| 低电量加强（默认 ≤ 20%） | 能效偏好 80，闲置关屏最多 2 分钟 |
| 电量回升到阈值 + 5% | 恢复日常模式 |
| 插电 | 切回原电源方案 |
| 外部程序切换方案 | 停止调度，尊重外部选择 |

日常闲置关屏默认 5 分钟，可设 1–30 分钟。能效偏好不是 CPU 使用率上限，也不是省电百分比。CPU 峰值设置保留，但偏向节能可能影响响应速度。固件、驱动和 Windows 电源模式会影响实际效果。

应用复制当前电源方案，仅修改副本中本机实际支持的电池设置。恢复记录位于 `%LOCALAPPDATA%\BatteryPilot\recovery.txt`。异常退出后副本可能继续生效；重新打开、覆盖安装或卸载都会尝试恢复。失败时保留记录并提示，请不要删除该记录。程序不会修改充电上限、结束进程或设置开机启动。

设置保存在同目录的 `settings.xml`。会话曲线仅保留在内存中，最多 2880 个样本；需要留存时在关闭前导出。程序没有数据上传功能。

## 检查与测试

正式适配 Windows 11 22H2 或更新版本、x64 处理器和 .NET Framework 4.8/4.8.1。EPP、放电瓦数、容量和循环次数取决于 CPU、固件、OEM 驱动与 WMI 数据；缺少的能力会在设备页标明并自动降级。完整范围见 `适配说明.md`。

已验证：C# 编译；对支持的能效设置执行四档写入与回读；交流设置不变；原方案不变；恢复记录创建与清理；低电量阈值及回差；真实进程差分采样；动态设备信息和四页界面。

不同硬件仍需要社区兼容性反馈；未完成大规模设备矩阵、长时间续航对照、真实拔插电源和睡眠恢复测试，不能承诺固定续航提升。

## 源码与图标

`source/` 为源码，`BatteryPilot.csproj` 为标准 .NET Framework 4.8 项目，`build.ps1` 可使用系统 Framework 编译器快速构建，`build-release.ps1` 使用 Inno Setup 生成安装包。`BatteryPilot.svg` 是可编辑矢量图标，另附 PNG 和含多种尺寸的 ICO。源码采用 MIT 许可证。

技术依据：[Microsoft powercfg](https://learn.microsoft.com/zh-cn/windows-hardware/design/device-experiences/powercfg-command-line-options)、[处理器能效偏好](https://learn.microsoft.com/en-us/windows-hardware/customize/power-settings/options-for-perf-state-engine-perfenergypreference)、[处理器时间](https://learn.microsoft.com/en-us/dotnet/api/system.diagnostics.process.totalprocessortime)、[进程 I/O 计数](https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-getprocessiocounters)。




